/* execute une spirale sur l'écran */
Void main() {
    Number size = 1000;
    for(Number i = 0;(i < size);i = (i + 1))
    {
    Say "spiral setp ", i, " / ",size ;
    Forward (1 * i) CM;
    Rotate -10;
    };

}
